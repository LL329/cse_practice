//BAsis syntax,variable and DAta-types Chapter 

#include <stdio.h> //header file(head)

// body (All codes)
int main() {
  int number = 3;
  float pi = 3.14;
  char star = '*';
  printf("number = %d pi = %f star = %c",number,pi,star);
  return 0;
}
