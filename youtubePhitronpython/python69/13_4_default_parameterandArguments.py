# Default parameters and keyword Arguments in Function

def greetings(name = 'Rahim', age='23'): # default parameters
    print(f"Hello {name}. You are {age} years old")

greetings(age = 99, name = "Alex") #keyword's arguments

# ARguments is powerful than parameters