#include<bits/stdc++.h>
using namespace std;

void sum(int x, int y){
//   no return type 
// so print hare
// parameter done so argument below 
}

int main()
{
     // hare we can't print because no return type sum(function)
     // if function have parameter input will be hare
     // input hare because sum (function) has parameter
     // value asba na so we can direct call
    //  sum();
    return 0;

}