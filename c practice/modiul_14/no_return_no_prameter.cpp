#include<bits/stdc++.h>
using namespace std;

void sum(void){
    int a, b;
    scanf("%d %d",&a,&b);
    int s = a + b;
    printf("%d\n",s);
    // no return type
    // no parameter 
    // so input and output is hare
}

int main()
{
    // no parameter so no argument
    // only function call 
    sum(); 
    return 0;

    
}